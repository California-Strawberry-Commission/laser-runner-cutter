uint32 path_id
common_interfaces/Vector2 destination
# Time at which the laser should arrive at `destination`. A zero
# (default-constructed) value means to fire immediately.
builtin_interfaces/Time timestamp